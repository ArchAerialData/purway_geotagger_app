name: macos-build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-13
    env:
      MACOSX_DEPLOYMENT_TARGET: "13.0"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Build app + run tests
        run: bash scripts/ci/macos_build.sh

      - name: Sign + notarize (optional)
        if: ${{ secrets.MACOS_CERT_P12 != '' && secrets.MACOS_CERT_PASSWORD != '' && secrets.APPLE_KEY_ID != '' && secrets.APPLE_ISSUER_ID != '' && secrets.APPLE_API_KEY_P8 != '' }}
        run: bash scripts/ci/macos_sign_and_notarize.sh
        env:
          MACOS_CERT_P12: ${{ secrets.MACOS_CERT_P12 }}
          MACOS_CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
          APPLE_KEY_ID: ${{ secrets.APPLE_KEY_ID }}
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}

      - name: Package DMG (unsigned)
        if: ${{ !(secrets.MACOS_CERT_P12 != '' && secrets.MACOS_CERT_PASSWORD != '' && secrets.APPLE_KEY_ID != '' && secrets.APPLE_ISSUER_ID != '' && secrets.APPLE_API_KEY_P8 != '') }}
        run: bash scripts/ci/macos_package.sh

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: PurwayGeotagger-macos
          path: dist/PurwayGeotagger.dmg
          if-no-files-found: error
